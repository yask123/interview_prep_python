prices = [1, 5, 8, 9, 10, 17, 17, 20]

rod_length = len(prices)


def rod_length(prices, total_length):
	max_value = float('inf')

	for i in range(total_length):
		max_value
		return max(max_value, prices[i] + (prices, total_length - i - 1))
